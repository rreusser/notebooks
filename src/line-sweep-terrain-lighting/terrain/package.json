{
  "name": "terrain",
  "version": "1.0.0",
  "description": "Terrain tile fetching and processing for line-sweep lighting",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "esbuild index.js --bundle --format=esm --outfile=dist/terrain.js --platform=browser --external:sharp --external:webgpu",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pngjs": "^7.0.0",
    "sharp": "^0.34.5",
    "webgpu": "^0.3.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
